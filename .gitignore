# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config. And all fiiles in bundle cahce
/.bundle
/bundle_cache/*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
*.log

# Ignore uploaded files in development
/storage/*
!/storage/.keep

/node_modules
/yarn-error.log

# Ignore cypress data
/cypress/videos
/cypress/videos/*
/cypress/tmp
/cypress/node_modules
/cypress/screenshots
/cypress/screenshots/*

# Ignore testing data
/postgres-data
.byebug_history

/public/packs-test
/node_modules
yarn-debug.log*
.yarn-integrity

.DS_Store

# Log workflow files
!.github/*

# CREDENTIALS
# We will guarantee we ignore the production key, although the file should never be stored on anyones laptop
# We check in the encrypted credentials as well as the development credentials
# Nothing wrong with everyone having access to the development credentials :)
/config/credentials/production.key
# /config/credentials/development.key

*.code-workspace

# Our Excel filesw ith BOMs
/app/test/fixtures/files/~*

/config/credentials/staging.key
/docker-compose-localhost.yml
/cypress/downloads
/out/
/out/**

# ignore personal IDE data
.fleet/*
.idea/*
.vscode/*

/config/master.key
/test/factories/
/vendor/bundle
log/development.log
*.log
